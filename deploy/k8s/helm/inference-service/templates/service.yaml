apiVersion: v1
kind: Service
metadata:
  name: {{ include "apex-inference-service.fullname" . }}
  labels:
    {{- include "apex-inference-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "apex-inference-service.selectorLabels" . | nindent 4 }}